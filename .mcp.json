{
  "mcpServers": {
    "gemini": {
      "type": "stdio",
      "command": "node",
      "args": ["${GEMINI_MCP_PATH}/dist/index.js"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
